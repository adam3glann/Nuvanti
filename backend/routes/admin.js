import { Router } from 'express';
import { z } from 'zod';
import { query } from '../lib/db.js';
import { productPayload, toPublicProduct } from '../lib/catalog.js';
const router = Router();
const productFields = z.object({ slug: z.string().regex(/^[a-z0-9-]+$/).max(160), name: z.string().min(2).max(160), description: z.string().max(5000).optional(), price: z.coerce.number().min(0).optional(), priceCents: z.coerce.number().int().min(0).optional(), category: z.string().min(1).max(80), collection: z.string().max(80).nullable().optional(), images: z.array(z.string()).max(12).optional(), colors: z.array(z.string().max(40)).max(20).optional(), sizes: z.array(z.string().max(20)).max(20).optional(), inventory: z.union([z.coerce.number().int().min(0), z.record(z.coerce.number().int().min(0))]).optional(), status: z.enum(['active', 'draft']).optional(), isActive: z.boolean().optional(), badges: z.array(z.string().max(30)).optional(), featured: z.boolean().optional(), bestseller: z.boolean().optional(), newArrival: z.boolean().optional(), sku: z.string().max(100).optional(), compareAtPrice: z.coerce.number().min(0).nullable().optional() });
const productInput = productFields.refine((value) => value.price !== undefined || value.priceCents !== undefined, { message: 'Price is required.' });
const columns = 'id, slug, name, description, price_cents, category, collection, images, colors, sizes, inventory, is_active, metadata';
router.get('/dashboard', async (req, res) => { const { rows } = await query(`SELECT (SELECT count(*)::int FROM orders) AS "orders", (SELECT count(*)::int FROM products WHERE is_active) AS "products", (SELECT count(*)::int FROM users WHERE role = 'customer') AS "customers", (SELECT coalesce(sum(total_cents), 0)::bigint FROM orders WHERE status <> 'cancelled') AS "revenueCents"`); res.json(rows[0]); });
router.get('/products', async (req, res) => { const { rows } = await query(`SELECT ${columns} FROM products ORDER BY updated_at DESC`); res.json(rows.map(toPublicProduct)); });
router.get('/orders', async (req, res) => { const { rows } = await query(`SELECT o.id, o.status, o.total_cents AS "totalCents", o.created_at AS "createdAt", o.shipping_address AS "shippingAddress", u.email, u.name, coalesce(json_agg(json_build_object('productId', i.product_id, 'name', i.product_name, 'priceCents', i.unit_price_cents, 'quantity', i.quantity)) FILTER (WHERE i.id IS NOT NULL), '[]') AS items FROM orders o JOIN users u ON u.id = o.user_id LEFT JOIN order_items i ON i.order_id = o.id GROUP BY o.id, u.email, u.name ORDER BY o.created_at DESC LIMIT 100`); res.json(rows.map((row) => ({ id: `NV-${row.id}`, dbId: String(row.id), status: row.status, paymentStatus: row.status === 'paid' ? 'paid' : 'pending', total: Number(row.totalCents) / 100, totalCents: Number(row.totalCents), createdAt: row.createdAt, customer: { name: row.name, email: row.email }, shipping: row.shippingAddress, items: row.items, notes: [] }))); });
router.patch('/orders/:id', async (req, res) => { const status = z.enum(['pending', 'paid', 'fulfilled', 'cancelled']).parse(req.body?.status); const { rows } = await query('UPDATE orders SET status = $1 WHERE id = $2 RETURNING id, status', [status, req.params.id]); if (!rows[0]) return res.status(404).json({ error: 'Order not found.' }); res.json(rows[0]); });
router.post('/products', async (req, res) => { const p = productPayload(productInput.parse(req.body)); try { const { rows } = await query(`INSERT INTO products (slug,name,description,price_cents,category,collection,images,colors,sizes,inventory,is_active,metadata) VALUES ($1,$2,$3,$4,$5,$6,$7::jsonb,$8::jsonb,$9::jsonb,$10,$11,$12::jsonb) RETURNING ${columns}`, [p.slug,p.name,p.description,p.priceCents,p.category,p.collection,JSON.stringify(p.images),JSON.stringify(p.colors),JSON.stringify(p.sizes),p.inventory,p.isActive,JSON.stringify(p.metadata)]); res.status(201).json(toPublicProduct(rows[0])); } catch (error) { if (error.code === '23505') return res.status(409).json({ error: 'A product with that slug already exists.' }); throw error; } });
router.patch('/products/:id', async (req, res) => { const existing = await query(`SELECT ${columns} FROM products WHERE id = $1`, [req.params.id]); if (!existing.rows[0]) return res.status(404).json({ error: 'Product not found.' }); const current = toPublicProduct(existing.rows[0]); const p = productPayload(productInput.parse({ ...current, ...productFields.partial().parse(req.body) })); const { rows } = await query(`UPDATE products SET slug=$1,name=$2,description=$3,price_cents=$4,category=$5,collection=$6,images=$7::jsonb,colors=$8::jsonb,sizes=$9::jsonb,inventory=$10,is_active=$11,metadata=$12::jsonb,updated_at=NOW() WHERE id=$13 RETURNING ${columns}`, [p.slug,p.name,p.description,p.priceCents,p.category,p.collection,JSON.stringify(p.images),JSON.stringify(p.colors),JSON.stringify(p.sizes),p.inventory,p.isActive,JSON.stringify(p.metadata),req.params.id]); res.json(toPublicProduct(rows[0])); });
router.delete('/products/:id', async (req, res) => { const result = await query('DELETE FROM products WHERE id = $1', [req.params.id]); if (!result.rowCount) return res.status(404).json({ error: 'Product not found.' }); res.status(204).end(); });
export default router;
